"""Reusable contract test batteries — one per protocol (§8.1).

Any implementation (built-in or future backend) is validated by importing and
parametrizing the relevant battery against it. This is what keeps "pluggable"
honest.
"""
