# Read the Docs configuration. https://docs.readthedocs.io/en/stable/config-file/v2.html
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

mkdocs:
  configuration: mkdocs.yml

# mkdocstrings imports the package to read docstrings, so ipax must be installed.
# NumPy is pulled in so importable code paths resolve at build time.
python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
        - numpy
